<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Modules">
            <directory>./Modules/*/Tests/Feature</directory>
            <directory>./Modules/*/Tests/Unit</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>app</directory>
            <directory suffix=".php">./Modules</directory>
        </include>

        <exclude>
            <!-- Exclude things that don't need test coverage -->
            <directory suffix=".php">Modules/Tasks/database</directory>   <!-- Migrations, factories, seeders -->
            <directory suffix=".php">Modules/Tasks/resources</directory>  <!-- Views -->
            <directory suffix=".php">Modules/Tasks/config</directory>     <!-- Config files -->
            <directory suffix=".php">Modules/Tasks/routes</directory>     <!-- Route files -->
            <directory suffix=".php">Modules/Tasks/app/Models</directory> <!-- Models (if no logic needs testing) -->
            <directory suffix=".php">Modules/Tasks/tests</directory> <!-- Models (if no logic needs testing) -->
        </exclude>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_DATABASE" value="testing"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
